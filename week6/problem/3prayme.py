def solution(a):

    # 스타수열의 조건
    # 길이가 2이상의 짝수 (빈수열 X)
    # 같은 길이끼리의 부분 수열은 교집합인 원소가 1개 이상있어야한다.
    # 인접하는 원소는 같으면 안된다.

    max_length, tmp = 0, 1
    while tmp > a:
        a


    print(max_length)

    answer = -1
    return None


print(solution([0]))
print(solution([5,2,3,3,5,3]))
print(solution([0,3,3,0,7,2,0,2,2,0]))